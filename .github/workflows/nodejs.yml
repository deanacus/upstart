name: Node.js CI

on:
  push:
  pull_request:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - run: npm install
      working-directory: ./packages/upstart
    - run: npm run build --if-present
      working-directory: ./packages/upstart
    - run: npm run test
      working-directory: ./packages/upstart
    # Test Coverage. Need to get coverage up to snuff first
    # - run: npm run ci
    #   working-directory: ./packages/upstart
    - run: npm run lint
      working-directory: ./packages/upstart
      env:
        CI: true
