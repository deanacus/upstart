export const fonts = [
  `system-ui, sans-serif`,
  `inherit`,
  `Menlo, monospace`,
];

[
  fonts.body,
  fonts.heading,
  fonts.monospace,
] = fonts;

fonts.code = fonts.monospace;

export default fonts;